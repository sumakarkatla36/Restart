SDLC 
1.PLANNING
2.REQUIREMENT ANALYSIS
3.DESIGN
4.IMPLEMENT
5.TEST
6.DEPLOY
7.MAINTAIN

SDLC HAS THREE MODELS
-----------------------
1.WATERFALL MODEL code everything,test and deploy
2.AGILE MODEL--> code test deploy every 2 weeks
3.DEVOPS MODEL-->CONTINUOUS DEPLOYMENT code test deploy code test deploy


DEVOPS LIFECYCLE
------------------
CONTINUOUS DEVELOPMENT
CONTINUOUS TESTING
CONTINUOUS DEPLOYMENT
CONTINUOUS MONITORING
CONTINUOUS INTEGRATION

All the code is placed in git it is a central place for storing the code


DAY-3
------
client server architecture

client = which requestss a resource
server = the one who responds to resource

if the client and server are in the same network then only it will communicate each other

network 
---------
wifi 
router 
cables
modem
blutooth

devices can be a client or a server

there are different types of client server architecture
1.  1 tier architecture
2.  2 tier architecture
3.  3 tier architecture

1 tier architecture
-----------------------
whatever we are doing in our laptop is called local host or local machine

when client server are in the saame device then it is 1 tier architecture 
it cannot be used by others so 1 tier we cannot be used or it can be dead

client and server are in the same machine
we will separate the client and server then it will be multitier

2 tier architecture
------------------------------
there are two layers 
1. client layers
2. server layers

server has 

hardware
operating system
applications
if we take any systems this is the same things that have

evry application has a database in the backend
few databases are
mysql
sql 
oracle

applications will connect to the database

all this clients are connected to the server
server will connected to the clients appliaction will responds to the clients 

by using 2 tier architectuere there are several disadvantages
1.there are capacity issues between application and databases it will effect the client

2 tier architecture is not recommandable

application should be in one server
database should be in another server

production environment is where the clients are directly connecting

whenever a developer develops a code it will be done in dev environment
then it will go for qa environment or test environment
then it will go for pre prod environement
then it will go for prod environement


3 tier architecture
---------------------------
1.client server
2.database server
3.application server

all the clients are connected to appliaction server
application server will connect to database server




if two machines communicate with each other using ip address 

devices communicate with each other with ip adress and hostname
ip=internet protocal 

we need a webserver it will take the request and redirect to the application server

keep the appliaction server private
keep the database server private
keep the webserver as public

3 tier architectuere is good for production 

example of webservers are 
nginx
apche
httpd

day-4
DNS =Domain name server
it keeps tracks of all the hostnames and ip addresses
DNS Converts the hostnames and hostnames to ip address

browser will talk to DNS
browser will talk to local DNS
Every system has DNS local dns path is /etc/host
browser will check with local DNS 
if it has ip it will give back
now local dns will talk to root name servers
browser require host name and ip address
we will give host names
hostnames will bee remembered by dns
if the DNS doesnt have it will go for root name servers
if the RNS Doest have the information it will go for Top level domains


name servers will identifies the domain name 
name server will take to the soa(start of authority)

it will reach firewall the firewall stops the unauthorized access to the network

firewall will allow or it will deney the access to the website 

http=80/not a secure connection to the application
https=443/secure connection to the application
ssh=22/to connect to linux machines
RDP==3389/connect to the windows machines

how is the data is travelled in the network 
-->the data will travelling in the network in the form of datapackets
the datapackets contains information.
whatever the protocols allowed in the firewall only that protocol is allowed


Load balancer distribute the traffic to the servers 

load balancer will follow round robbin method


nslookup command is used to see the name of the website or ip address of the website


Day-5
----------------

Day-9
------------------
aws is a group of services
we can access AWS services through amazon Management Console

VM's = Instances

EC2-->Elastic compute cloud

Ec2 is a AWS service that creates a virtual machine 

Ec2 is a AWS service where we can Launch ec2 instances

in infrastrutcure as service customers are responsible for Application data and OS

EBS --> Elastic Bean stackk is used for easy and quick deployment
it is used as Platform as a Service

Day-10
----------
 -->increasing or decreasing the servers according to the Load is called as elasticity
 
 -->Elasticity is shaort term
 
 -->Elastiicity can be achieved in aws using Auto-Scaling
 
 Auto-Scaling=increasing and Decreasing of the servers are called Autoscaling
 
Elasticity is called Horizontal Scaling

use the same capacity of the servers in auto-scaling

Scalability
------------------------------------
if you increasing the capcity of the server then it is called as Scalanility

if you incraese the no of servers then it is called as Elasticity

Scalability =  Scaleup/scale down

Scalability is also called as vertical Scaling

Scalability is Long Term

Scalability can be achieved in AWS By changing the instance Type

Instance type = Memory + CPU

High Availability
--------------------------
the period of time the service availlable to the customer is called High Availability

the period of time the service is Not Available to the customer is called Downtime


Availability is measured in percentage

Redundancy = Duplicate/having the same application on different servers

Load balancer will health check to the appliaction not server

Monittoring = Load balancer will cheeck if the application is reachable or not using health checks

Failover = if one server goes down ,other servers will take the requests sent by LB

Day-11
-------------------------------------
 Regions and Availabilty zzones
 
AWS has global infrastrutcure

Region is a place where AWS Has its infrastructure

A region has multiple Datacenters

A region has multiple AZ's

Servers = instances

servers/instances are placed in AZ's

AZ's are sync with each other (network),not data

best practice is to distribute the instances across multiple  AZ'S 

Regions and availability zones

Region = its a geo graphical Area,EX AWS-Region 




























































































































